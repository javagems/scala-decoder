require 'rubygems'
require 'rake'

begin
  require 'jeweler'
  Jeweler::Tasks.new do |gem|
    gem.name = "scala-decoder"

    gem.summary = %Q{A JavaGem version of scala-decoder - packaged by JavaGems (http://www.javagems.org) - original by LAMP/EPFL (http://lamp.epfl.ch/)}



    gem.description = %Q{scala-decoder packaged up in a JavaGem}

    gem.homepage = "http://www.javagems.org/"

    gem.authors = ["pending/unknown"]

    gem.email = "autobuild@javagems.org"
    gem.version = "2.7.1"

  

    gem.add_development_dependency "scala-library", "~>2.7.1"

    gem.add_development_dependency "scala-compiler", "~>2.7.1"

  end
rescue LoadError
  puts "Jeweler (or a dependency) not available. Install it with: sudo gem install jeweler"
end

task :default => :build
